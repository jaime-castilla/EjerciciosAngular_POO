export class Campos{

    //Atributos
    x: number = 0


    //Métodos
    muestra(numero1: number, numero2: number): number{

        return this.x = numero1 + numero2

    }

    incrementa(): number{

        this.x++
        return this.x

    }

    //Constructores
    //Getters y Setters
}